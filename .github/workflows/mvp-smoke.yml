name: VoxelCore MVP Smoke Tests

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  smoke:
    name: Minecraft ${{ matrix.minecraft }} MVP smoke
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - minecraft: '1.12.2'
            module: voxelcore-v1_12
            image: itzg/minecraft-server:java8
            server_type: SPIGOT
          - minecraft: '1.21.4'
            module: voxelcore-v1_21_4
            image: itzg/minecraft-server:java21
            server_type: PAPER

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21
          cache: maven

      - name: Build target distribution
        run: mvn -U -pl "${{ matrix.module }}" -am package

      - name: Run real-server MVP smoke test
        run: bash .github/scripts/mvp-smoke.sh "${{ matrix.minecraft }}" "${{ matrix.module }}" "${{ matrix.image }}" "${{ matrix.server_type }}"

      - name: Upload smoke server logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: smoke-${{ matrix.minecraft }}-server-data
          path: .smoke/${{ matrix.minecraft }}/logs/
          if-no-files-found: ignore
